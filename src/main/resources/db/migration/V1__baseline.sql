-- =====================================================================
-- Baseline do Flyway
-- =====================================================================
-- As tabelas deste projeto foram criadas originalmente via ddl-auto do 
-- Hibernate. Este script marca o ponto de partida das migrações versionadas.
-- NÃO adicione CREATE TABLE aqui — o schema já existe no banco.
--
-- A partir de V2__, toda alteração de schema deve ser feita com novos 
-- arquivos de migração (ex: V2__adicionar_campo_x.sql).
-- =====================================================================

SELECT 1;
